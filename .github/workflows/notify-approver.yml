name: Notify Approver

on:
  pull_request:
    types: [opened, labeled]

permissions:
  contents: read

jobs:
  notify:
    uses: firelock-ai/kin-actions/.github/workflows/notify-approver.yml@v0.1.30
    permissions:
      contents: read
      pull-requests: write
