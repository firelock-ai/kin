<?xml version="1.0" encoding="UTF-8"?>
<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Copyright 2026 Firelock, LLC -->
<!--
  launchd plist template for kin-daemon (per-repo).

  This is a TEMPLATE. `kin init` generates a repo-specific plist with
  the correct --repo path and --port. See lifecycle.rs for the code.

  Manual install (single repo):
    cp ai.firelock.kin-daemon.plist ~/Library/LaunchAgents/
    launchctl load ~/Library/LaunchAgents/ai.firelock.kin-daemon.plist

  Automatic install:
    `kin init .` registers the daemon with launchd automatically.
    `kin eject` removes it.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>ai.firelock.kin-daemon.REPO_ID</string>

    <key>ProgramArguments</key>
    <array>
        <string>DAEMON_BIN</string>
        <string>--repo</string>
        <string>REPO_PATH</string>
        <string>--port</string>
        <string>PORT</string>
    </array>

    <key>KeepAlive</key>
    <true/>

    <key>RunAtLoad</key>
    <true/>

    <key>ProcessType</key>
    <string>Background</string>

    <key>StandardOutPath</key>
    <string>/tmp/kin-daemon-REPO_ID.stdout.log</string>

    <key>StandardErrorPath</key>
    <string>/tmp/kin-daemon-REPO_ID.stderr.log</string>
</dict>
</plist>
