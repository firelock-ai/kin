target/
.git/
.kin/
# Keep the tracked cargo config in the build context; it carries the registry
# alias and public Git pins required until every kin-* crate is published.
node_modules/
*.log
.pre-commit-config.yaml
.github/
plugins/
packages/*/node_modules/
